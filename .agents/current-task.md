# 当前任务

## 进行中

- 2026-06-26：视频访问性能优化
  - 目标：把剧集播放入口从 `/api/cines/episodes/:id/stream` 迁移到 `/media/videos/:id`，并加固 HTTP Range、HEAD 与缓存友好响应头。
  - 范围：
    - 后端新增/调整公开媒体路由
    - 服务端 Range 异常语义与响应头完善
    - 客户端媒体 URL 解析适配 `/media/*`
    - 补充测试与 README
  - 非目标：
    - 不实现 Nginx 缓存
    - 不改动视频目录结构
  - 当前进度：
    - 阶段 1 已完成
    - 阶段 2A HLS 已完成
    - 阶段 2A 收尾稳态修复已完成：删除影视会清理 HLS 目录；已有可用 HLS 时，补生成某档失败不会误伤旧档位
    - 阶段 2A 异步任务化已完成：HLS 生成接口已改为 Redis + BullMQ 后台任务队列，管理端已接轮询、独立 HLS 设置行布局与 HLS 说明弹窗
    - 阶段 2B 多档自适应已完成并人工验收通过：默认 HLS 已改为按源视频分辨率串行生成自适应档位集，并保留单档补生成功能；当前已确认可成功构建 HLS，客户端也可正常观看
    - 播放页缓冲提示与移动端 seek 交互修复已完成：视频首帧加载、等待缓冲时会展示转圈 loading，用户拖动进度条时改为预览后松手提交，不再被遮罩打断
    - 真机总时长兜底已完成：播放页在原生 HLS 时长异常时会回退使用后端 `duration_seconds`，避免总时长显示为 `00:00`
    - 真机续播恢复时机修复已完成：续播成功改为基于媒体真实 seek 结果判定，避免先跳到历史位置又回到开头
    - 阶段 3 Nginx 缓存尚未开始

### 最近完成

- 2026-06-25：客户端真实观看进度与续播恢复，已归档到 `.agents/archive/2026-06-25-client-real-watch-progress.md`。

### 状态

- 阶段 2B 已完成且人工验收通过，等待是否继续进入阶段 3 Nginx 缓存。
