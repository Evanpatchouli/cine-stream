# 归档任务：客户端 mock 真实接口替换

## 目标

将 `client` 中依赖 mock 数据和原生 `fetch` 的实现替换为真实后端接口；客户端 API 封装对齐 `admin` 的 axios 模式；后端补齐客户端页面需要但尚不存在的接口与字段。

## 完成情况

- [x] 后端扩展影视与剧集展示字段。
- [x] 后端新增用户资料、观看历史、收藏、个人概览接口。
- [x] 客户端 API 层改为 axios，并拆分为用户、影视、观看数据接口模块。
- [x] 客户端页面和 store 移除 mock 数据依赖，改用真实接口和空状态。
- [x] 构建验证与 BOM 检查完成。
