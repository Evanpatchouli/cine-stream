# 归档任务：管理端影视图片 OSS 上传

## 目标

管理端创建/编辑影视时，海报、背景图和剧集缩略图改为选择图片上传；后端封装 `ali-oss-server` SDK，内部维护 token 刷新和上传调用；新增配置项支持通过 env 设置 ali-oss-server 地址、凭证和刷新间隔。

## 完成情况

- [x] 审计 `E:\ali-oss-server` token 与上传接口。
- [x] 后端 SDK、配置与上传接口实现。
- [x] 管理端弹窗高度限制与图片上传/手动输入 UI 实现。
- [x] 构建验证、BOM 检查和交接文档更新。
