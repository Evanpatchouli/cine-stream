# 归档任务：播放页剧集写入路径

## 目标

播放页刷新后保持当前剧集，不再回到第一集；当前剧集 ID 集成到 URL 路径中。

## 完成情况

- [x] 新增 `/play/:id/:episodeId` 路由，并保留 `/play/:id` 入口。
- [x] 播放页从路径读取当前剧集，缺失或无效时自动补齐到有效剧集路径。
- [x] 点击剧集时更新路径，刷新后仍停留当前集。
- [x] 继续观看和历史记录入口优先携带 `episode_id`。
- [x] `pnpm --filter @cine-stream/client build` 通过。
- [x] 本次触碰文本文件 BOM 检查通过。
